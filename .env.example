# -----------------------------------------------------------------------------
# Application Configuration
# -----------------------------------------------------------------------------
# The base URL of the application
# Used for generating absolute URLs, handling OAuth redirects, and other app-wide references
# Default is localhost for local development
NEXT_PUBLIC_APP_URL=http://localhost:3000

# -----------------------------------------------------------------------------
# Database Configuration
# -----------------------------------------------------------------------------
# MySQL Database Credentials
# 
# IMPORTANT:
# - Never commit sensitive credentials to version control
# - Use separate configurations for development, staging, and production
# - Store secrets securely using environment variables or a secrets manager
#
# Connection details:
# - NEXT_PUBLIC_DATABASE_HOST: The hostname or IP address of the MySQL server
# - NEXT_PUBLIC_DATABASE_USER: The database username
# - NEXT_PUBLIC_DATABASE_PASSWORD: The database user's password (leave empty for no password)
# - NEXT_PUBLIC_DATABASE_NAME: The name of the database to connect to
# - NEXT_PUBLIC_DATABASE_PORT: The port MySQL is running on (default: 3306)

NEXT_PUBLIC_DATABASE_HOST=localhost
NEXT_PUBLIC_DATABASE_USER=username
NEXT_PUBLIC_DATABASE_PASSWORD=password
NEXT_PUBLIC_DATABASE_NAME=databasename
NEXT_PUBLIC_DATABASE_PORT=3306